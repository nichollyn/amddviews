<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item
        android:id="@+id/work_mode"
        android:checked="true"
        android:showAsAction="always|withText"
        android:title="@string/work_mode">
        <menu>
            <group
                android:id="@+id/mode_group"
                android:checkableBehavior="single"
                android:enabled="true"
                android:menuCategory="container"
                android:visible="true" >
                <item
                    android:id="@+id/mode_action_mode"
                    android:showAsAction="always|withText"
                    android:title="@string/work_in_action_mode" android:checked="true"/>
                <item
                    android:id="@+id/mode_normal_mode"
                    android:showAsAction="always|withText"
                    android:title="@string/work_in_normal_mode"/>
            </group>
        </menu>
    </item>

</menu>